services:
  sevelr-remote:
    build: .
    container_name: sevelr-remote
    restart: always
    ports:
      - "5000:5000"
    environment:
      - SEVELR_SECRET=Fu2525252525
    volumes:
      - ./data:/app/data
